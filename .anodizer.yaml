# yaml-language-server: $schema=https://tj-smith47.github.io/anodizer/schema.json
# Anodizer dogfooding config — exercises as many features as possible.
# Templates use Tera syntax with Go-style compat.
#   - Pipe filters: {{ Tag | trimprefix(prefix='v') }}
#   - Variables: {{ RawVersion }}, {{ Branch }}, {{ CommitDate }}, etc.

version: 2
project_name: anodizer
dist: ./dist

# Show artifact file sizes after build (confirms report_sizes wiring)
report_sizes: true

# Top-level env vars available to all template expressions
env:
  - RELEASE_TYPE=stable

# Custom template variables accessible as {{ Var.repo_url }}
variables:
  repo_url: "https://github.com/tj-smith47/anodizer"
  description: "A Rust-native release automation tool"

# Git tag discovery settings
git:
  tag_sort: "-version:refname"

# Auto-tagging configuration (used by `anodizer tag`)
tag:
  default_bump: patch
  tag_prefix: "v"
  release_branches:
    - main
    - master
  initial_version: "0.1.0"

# Project metadata written to dist/metadata.json
metadata:
  description: "A Rust-native release automation tool"
  homepage: "https://github.com/tj-smith47/anodizer"
  license: MIT
  maintainers:
    - "TJ Smith"
  mod_timestamp: "{{ CommitTimestamp }}"

defaults:
  targets:
    - x86_64-unknown-linux-gnu
    - aarch64-unknown-linux-gnu
    - x86_64-apple-darwin
    - aarch64-apple-darwin
    - x86_64-pc-windows-msvc
    - aarch64-pc-windows-msvc
  cross: auto
  flags: --release
  archives:
    format: tar.gz
    format_overrides:
      - os: windows
        format: zip
  checksum:
    algorithm: sha256

before:
  hooks:
    - cargo fmt --check
    - cargo clippy -- -D warnings
    - cargo test

after:
  post:
    - echo "Release complete for {{ ProjectName }} {{ Tag }}"

crates:
  # --- Tier 1: core (no workspace deps) ---
  - name: anodizer-core
    path: crates/core
    tag_template: "v{{ Version }}"
    publish:
      crates:
        enabled: true
        index_timeout: 300

  # --- Tier 2: stage crates (depend on core) ---
  - name: anodizer-stage-build
    path: crates/stage-build
    tag_template: "v{{ Version }}"
    depends_on: [anodizer-core]
    publish:
      crates: { enabled: true }
  - name: anodizer-stage-archive
    path: crates/stage-archive
    tag_template: "v{{ Version }}"
    depends_on: [anodizer-core]
    publish:
      crates: { enabled: true }
  - name: anodizer-stage-nfpm
    path: crates/stage-nfpm
    tag_template: "v{{ Version }}"
    depends_on: [anodizer-core]
    publish:
      crates: { enabled: true }
  - name: anodizer-stage-checksum
    path: crates/stage-checksum
    tag_template: "v{{ Version }}"
    depends_on: [anodizer-core]
    publish:
      crates: { enabled: true }
  - name: anodizer-stage-changelog
    path: crates/stage-changelog
    tag_template: "v{{ Version }}"
    depends_on: [anodizer-core]
    publish:
      crates: { enabled: true }
  - name: anodizer-stage-release
    path: crates/stage-release
    tag_template: "v{{ Version }}"
    depends_on: [anodizer-core]
    publish:
      crates: { enabled: true }
  - name: anodizer-stage-publish
    path: crates/stage-publish
    tag_template: "v{{ Version }}"
    depends_on: [anodizer-core]
    publish:
      crates: { enabled: true }
  - name: anodizer-stage-docker
    path: crates/stage-docker
    tag_template: "v{{ Version }}"
    depends_on: [anodizer-core]
    publish:
      crates: { enabled: true }
  - name: anodizer-stage-sign
    path: crates/stage-sign
    tag_template: "v{{ Version }}"
    depends_on: [anodizer-core]
    publish:
      crates: { enabled: true }
  - name: anodizer-stage-announce
    path: crates/stage-announce
    tag_template: "v{{ Version }}"
    depends_on: [anodizer-core]
    publish:
      crates: { enabled: true }
  - name: anodizer-stage-source
    path: crates/stage-source
    tag_template: "v{{ Version }}"
    depends_on: [anodizer-core]
    publish:
      crates: { enabled: true }
  - name: anodizer-stage-upx
    path: crates/stage-upx
    tag_template: "v{{ Version }}"
    depends_on: [anodizer-core]
    publish:
      crates: { enabled: true }
  - name: anodizer-stage-snapcraft
    path: crates/stage-snapcraft
    tag_template: "v{{ Version }}"
    depends_on: [anodizer-core]
    publish:
      crates: { enabled: true }
  - name: anodizer-stage-dmg
    path: crates/stage-dmg
    tag_template: "v{{ Version }}"
    depends_on: [anodizer-core]
    publish:
      crates: { enabled: true }
  - name: anodizer-stage-msi
    path: crates/stage-msi
    tag_template: "v{{ Version }}"
    depends_on: [anodizer-core]
    publish:
      crates: { enabled: true }
  - name: anodizer-stage-pkg
    path: crates/stage-pkg
    tag_template: "v{{ Version }}"
    depends_on: [anodizer-core]
    publish:
      crates: { enabled: true }
  - name: anodizer-stage-blob
    path: crates/stage-blob
    tag_template: "v{{ Version }}"
    depends_on: [anodizer-core]
    publish:
      crates: { enabled: true }
  - name: anodizer-stage-nsis
    path: crates/stage-nsis
    tag_template: "v{{ Version }}"
    depends_on: [anodizer-core]
    publish:
      crates: { enabled: true }
  - name: anodizer-stage-appbundle
    path: crates/stage-appbundle
    tag_template: "v{{ Version }}"
    depends_on: [anodizer-core]
    publish:
      crates: { enabled: true }
  - name: anodizer-stage-flatpak
    path: crates/stage-flatpak
    tag_template: "v{{ Version }}"
    depends_on: [anodizer-core]
    publish:
      crates: { enabled: true }
  - name: anodizer-stage-notarize
    path: crates/stage-notarize
    tag_template: "v{{ Version }}"
    depends_on: [anodizer-core]
    publish:
      crates: { enabled: true }
  - name: anodizer-stage-templatefiles
    path: crates/stage-templatefiles
    tag_template: "v{{ Version }}"
    depends_on: [anodizer-core]
    publish:
      crates: { enabled: true }
  - name: anodizer-stage-makeself
    path: crates/stage-makeself
    tag_template: "v{{ Version }}"
    depends_on: [anodizer-core]
    publish:
      crates: { enabled: true }
  - name: anodizer-stage-srpm
    path: crates/stage-srpm
    tag_template: "v{{ Version }}"
    depends_on: [anodizer-core]
    publish:
      crates: { enabled: true }
  - name: anodizer-stage-sbom
    path: crates/stage-sbom
    tag_template: "v{{ Version }}"
    depends_on: [anodizer-core]
    publish:
      crates: { enabled: true }

  # --- Tier 3: CLI binary (depends on all stages) ---
  - name: anodizer
    path: crates/cli
    tag_template: "v{{ Version }}"
    depends_on:
      - anodizer-core
      - anodizer-stage-build
      - anodizer-stage-archive
      - anodizer-stage-nfpm
      - anodizer-stage-checksum
      - anodizer-stage-changelog
      - anodizer-stage-release
      - anodizer-stage-publish
      - anodizer-stage-docker
      - anodizer-stage-sign
      - anodizer-stage-announce
      - anodizer-stage-source
      - anodizer-stage-upx
      - anodizer-stage-snapcraft
      - anodizer-stage-dmg
      - anodizer-stage-msi
      - anodizer-stage-pkg
      - anodizer-stage-blob
      - anodizer-stage-nsis
      - anodizer-stage-appbundle
      - anodizer-stage-flatpak
      - anodizer-stage-notarize
      - anodizer-stage-templatefiles
      - anodizer-stage-makeself
      - anodizer-stage-srpm
      - anodizer-stage-sbom
    builds:
      - binary: anodizer
        features: []
        no_default_features: false
    archives:
      - name_template: "{{ ProjectName }}-{{ Version }}-{{ Os }}-{{ Arch }}"
        format: tar.gz
        format_overrides:
          - os: windows
            format: zip
        files:
          - LICENSE
          - README.md
          - docs/configuration.md
    checksum:
      name_template: "{{ ProjectName }}-{{ Version }}-checksums.txt"
      algorithm: sha256
    binstall:
      enabled: true
      pkg_url: "https://github.com/tj-smith47/anodizer/releases/download/v{{ Version }}/anodizer-{{ Version }}-{ target }.tar.gz"
      bin_dir: ""
      pkg_fmt: tgz
    release:
      github:
        owner: tj-smith47
        name: anodizer
      draft: false
      prerelease: auto
      make_latest: auto
      name_template: "{{ ProjectName }} {{ Tag }}"
      footer: |
        ---
        Released with [anodizer](https://github.com/tj-smith47/anodizer) 🦀
      include_meta: true
    publish:
      crates:
        enabled: true
        index_timeout: 300
      homebrew:
        tap:
          owner: tj-smith47
          name: homebrew-tap
        directory: Formula
        description: "A Rust-native release automation tool"
        license: MIT
        install: |
          bin.install "anodizer"
        test: |
          system "#{bin}/anodizer", "--version"
        commit_author:
          name: TJ Smith
          email: tj@jarvispro.io
      scoop:
        bucket:
          owner: tj-smith47
          name: scoop-bucket
        description: "A Rust-native release automation tool"
        license: MIT
        commit_author:
          name: TJ Smith
          email: tj@jarvispro.io
      chocolatey:
        project_repo:
          owner: tj-smith47
          name: anodizer
        description: "A Rust-native release automation tool"
        license: MIT
        tags:
          - release
          - automation
          - rust
        commit_author:
          name: TJ Smith
          email: tj@jarvispro.io
      winget:
        manifests_repo:
          owner: tj-smith47
          name: winget-pkgs
        description: "A Rust-native release automation tool"
        license: MIT
        package_identifier: "TJSmith.Anodizer"
        publisher: "TJ Smith"
        # Rust/MSVC binaries dynamically link against the VC++ 2015+
        # runtime. Without this dependency declared, winget's automated
        # validation fails at invocation time with STATUS_DLL_NOT_FOUND
        # (exit code -1073741515).
        dependencies:
          - package_identifier: "Microsoft.VCRedist.2015+.x64"
        commit_author:
          name: TJ Smith
          email: tj@jarvispro.io
      aur:
        disable: true  # no AUR account registered yet
        git_url: "ssh://aur@aur.archlinux.org/anodizer-bin.git"
        package_name: anodizer-bin
        description: "A Rust-native release automation tool"
        license: MIT
        depends:
          - glibc
        commit_author:
          name: TJ Smith
          email: tj@jarvispro.io
      nix:
        name: anodizer
        repository:
          owner: tj-smith47
          name: nix-pkgs
        homepage: "https://github.com/tj-smith47/anodizer"
        description: "A Rust-native release automation tool"
        license: mit
        skip_upload: false
        commit_author:
          name: TJ Smith
          email: tj@jarvispro.io
      krew:
        disable: true  # anodizer is not a kubectl plugin — krew is not an appropriate channel
        manifests_repo:
          owner: tj-smith47
          name: krew-index
        short_description: "A Rust-native release automation tool"
        homepage: "https://github.com/tj-smith47/anodizer"
        commit_author:
          name: TJ Smith
          email: tj@jarvispro.io
    nfpm:
      - package_name: anodizer
        formats:
          - deb
          - rpm
          - apk
        vendor: "TJ Smith"
        homepage: "https://github.com/tj-smith47/anodizer"
        maintainer: "TJ Smith <tj@example.com>"
        description: "A Rust-native release automation tool"
        license: MIT
        bindir: /usr/bin
        file_name_template: "{{ ProjectName }}_{{ RawVersion }}_{{ Os }}_{{ Arch }}"
        contents:
          - src: LICENSE
            dst: /usr/share/doc/anodizer/LICENSE
          - src: README.md
            dst: /usr/share/doc/anodizer/README.md
    snapcrafts:
      - name: anodizer
        summary: "A Rust-native release automation tool"
        description: |
          Anodizer is a release automation tool for Rust projects, inspired by
          GoReleaser. It handles building, packaging, publishing, and announcing
          releases across multiple platforms and package managers.
        grade: stable
        confinement: strict
        license: MIT
        publish: true
        apps:
          anodizer:
            command: anodizer
            plugs:
              - home
              - network
              - network-bind
    flatpaks:
      - disable: true  # disabled — no Flatpak runtime configured yet
        app_id: io.github.tj_smith47.Anodizer
        runtime: org.freedesktop.Platform
        runtime_version: "24.08"
        sdk: org.freedesktop.Sdk
        command: anodizer
        finish_args:
          - "--share=network"
          - "--filesystem=host"
    app_bundles:
      - disable: true  # disabled — no macOS app bundle identity configured
        bundle: io.github.tj-smith47.anodizer
        name: Anodizer
    dmgs:
      - disable: true  # disabled — no macOS DMG configured (needs app_bundle first)
        name: Anodizer
    pkgs:
      - disable: true  # disabled — no macOS PKG signing identity configured
        identifier: io.github.tj-smith47.anodizer
    msis:
      - disable: true  # disabled — no WiX source file (.wxs) created
        name: Anodizer
    nsis:
      - disable: true  # disabled — no NSIS script created
        name: Anodizer
    blobs:
      - disable: true  # disabled — no cloud storage configured
        provider: s3
        bucket: anodizer-releases

notarize:
  disable: true  # disabled — no Apple Developer credentials configured

template_files:
  - id: install-script
    src: scripts/install.sh.tpl
    dst: install.sh
    mode: "0755"

changelog:
  header: "# Changelog for {{ ProjectName }}"
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^ci:"
      - "^chore:"
      - "^style:"
  groups:
    - title: Features
      regexp: "^feat"
      order: 0
    - title: Bug Fixes
      regexp: "^fix"
      order: 1
    - title: Performance
      regexp: "^perf"
      order: 2
    - title: Others
      order: 999

signs:
  - id: default
    artifacts: checksum
    cmd: gpg
    args:
      - "--batch"
      - "--local-user"
      - "{{ Env.GPG_FINGERPRINT }}"
      - "--output"
      - "{{ Signature }}"
      - "--detach-sig"
      - "{{ Artifact }}"

source:
  enabled: true
  format: tar.gz
  name_template: "{{ ProjectName }}-{{ Version }}-source"
  files:
    - "crates/**/*.rs"
    - "Cargo.toml"
    - "Cargo.lock"
    - "LICENSE"
    - "README.md"

sbom:
  enabled: true
  format: cyclonedx

upx:
  - id: default
    enabled: true
    binary: upx
    args:
      - "--best"
      - "--lzma"
    # UPX doesn't support macOS ARM or Windows ARM binaries
    targets:
      - "x86_64-unknown-linux-gnu"
      - "aarch64-unknown-linux-gnu"
      - "x86_64-apple-darwin"
      - "x86_64-pc-windows-msvc"

nightly:
  name_template: "{{ ProjectName }}-nightly"
  tag_name: nightly

snapshot:
  name_template: "{{ Tag | trimprefix(prefix='v') }}-SNAPSHOT-{{ ShortCommit }}"

# Split/merge CI configuration for fan-out builds
partial:
  by: goos

# Binary-level signing (separate from archive/checksum signing above)
binary_signs:
  - id: default
    artifacts: binary
    cmd: cosign
    args:
      - "sign-blob"
      - "--key=env://COSIGN_KEY"
      - "--bundle={{ Signature }}"
      - "--yes"
      - "{{ Artifact }}"

# Docker image signing
docker_signs:
  - id: default
    artifacts: manifests
    cmd: cosign
    args:
      - "sign"
      - "--key=env://COSIGN_KEY"
      - "--yes"
      - "{{ Artifact }}"

# Milestone closing — close GitHub milestones matching the release tag
milestones:
  - repo:
      owner: tj-smith47
      name: anodizer
    close: false  # disabled — no milestones configured yet
    fail_on_error: false
    name_template: "{{ Tag }}"

# DockerHub description sync
dockerhub:
  - disable: true  # disabled — no DockerHub image for anodizer
    username: "{{ Env.DOCKERHUB_USERNAME }}"
    description: "A Rust-native release automation tool"

# Artifactory upload configurations
artifactories:
  - name: production
    disable: true  # disabled — no Artifactory instance
    target: "https://artifactory.example.com/generic-local/anodizer/{{ Tag }}/"
    method: PUT

# CloudSmith publisher
cloudsmiths:
  - organization: jarvispro
    repository: anodizer
    formats: [deb, rpm, alpine]
    distributions:
      deb: any-distro/any-version
      rpm: any-distro/any-version
      alpine: alpine/any-version

# Note: `fury:` and `npms:` publishers removed 2026-04-16 (see .claude/known-bugs.md);
# users migrating should replace fury with cloudsmiths/artifactories and drop npms entirely.

# Source RPM configuration
srpm:
  enabled: true
  package_name: anodizer
  spec_file: anodizer.spec
  file_name_template: "{{ ProjectName }}-{{ RawVersion }}-1.src.rpm"

# Makeself self-extracting archives
makeselfs:
  - id: default
    name: "Anodizer Installer"
    script: scripts/makeself-install.sh
    name_template: "{{ ProjectName }}-{{ Version }}-{{ Os }}-{{ Arch }}-installer.run"

announce:
  discord:
    enabled: false
    webhook_url: "{{ Env.DISCORD_WEBHOOK_URL }}"
    message_template: "{{ ProjectName }} {{ Tag }} released! {{ ReleaseURL }}"
  discourse:
    enabled: false
    url: "{{ Env.DISCOURSE_URL }}"
    api_key: "{{ Env.DISCOURSE_API_KEY }}"
    category: releases
    title_template: "{{ ProjectName }} {{ Tag }} released"
  slack:
    enabled: false
    webhook_url: "{{ Env.SLACK_WEBHOOK_URL }}"
    message_template: "{{ ProjectName }} {{ Tag }} released! {{ ReleaseURL }}"
  webhook:
    enabled: true
    endpoint_url: "https://jarvispro.io/webhooks/anodizer"
    content_type: application/json
    message_template: '{"project":"{{ ProjectName }}","tag":"{{ Tag }}","url":"{{ ReleaseURL }}"}'
  telegram:
    enabled: false
    bot_token: "{{ Env.TELEGRAM_BOT_TOKEN }}"
    chat_id: "{{ Env.TELEGRAM_CHAT_ID }}"
    message_template: "{{ ProjectName }} {{ Tag }} released! {{ ReleaseURL }}"
    parse_mode: MarkdownV2
  teams:
    enabled: false
    webhook_url: "{{ Env.TEAMS_WEBHOOK_URL }}"
    message_template: "{{ ProjectName }} {{ Tag }} released! {{ ReleaseURL }}"
  mattermost:
    enabled: false
    webhook_url: "{{ Env.MATTERMOST_WEBHOOK_URL }}"
    channel: releases
    username: release-bot
    message_template: "{{ ProjectName }} {{ Tag }} released! {{ ReleaseURL }}"
  email:
    enabled: true
    host: smtp.gmail.com
    port: 587
    username: toss45@gmail.com
    from: toss45@gmail.com
    to:
      - "tj@jarvispro.io"
    subject_template: "{{ ProjectName }} {{ Tag }} released"
    message_template: "{{ ProjectName }} {{ Tag }} is now available at {{ ReleaseURL }}"
  reddit:
    enabled: false
    title_template: "{{ ProjectName }} {{ Tag }} released"
    url: "{{ ReleaseURL }}"
  twitter:
    enabled: false
    message_template: "{{ ProjectName }} {{ Tag }} released! {{ ReleaseURL }}"
  mastodon:
    enabled: false
    server: "{{ Env.MASTODON_SERVER }}"
    message_template: "{{ ProjectName }} {{ Tag }} released! {{ ReleaseURL }}"
  bluesky:
    enabled: false
    username: "{{ Env.BLUESKY_USERNAME }}"
    message_template: "{{ ProjectName }} {{ Tag }} released! {{ ReleaseURL }}"
  linkedin:
    enabled: false
    message_template: "{{ ProjectName }} {{ Tag }} released! {{ ReleaseURL }}"
  opencollective:
    enabled: false
    slug: "{{ Env.OPENCOLLECTIVE_SLUG }}"
    title_template: "{{ ProjectName }} {{ Tag }} released"
