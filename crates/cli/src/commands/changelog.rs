use std::path::Path;
use anyhow::Result;

pub fn run(_crate_name: Option<String>, _config_override: Option<&Path>) -> Result<()> {
    eprintln!("changelog command not yet implemented");
    Ok(())
}
