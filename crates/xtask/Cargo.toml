[package]
name = "xtask"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
anodize = { path = "../cli" }
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
tera.workspace = true
