/target/
/dist/
**/dist/
.task/
.worktrees/
.det-worktrees/
.det-tmp/
.worktreeinclude
.superpowers/
.zig-cache/
docs/site/public/
CLAUDE.local.md
# Ignore everything under .claude/ except the two load-bearing CI scripts.
# `.claude/*` (not `.claude/`) keeps the directory itself traversable so
# the per-file re-includes below are actually evaluated.
.claude/*
!.claude/scripts/
.claude/scripts/*
!.claude/scripts/coverage-percent.sh
!.claude/scripts/publish-coverage.sh
# Coverage artifact emitted by `task coverage:check`.
cobertura.xml
